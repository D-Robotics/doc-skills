# 技术文档写作规范

> **适用对象**：为文档站写内容的所有人——研发按规范写初稿（深度运营由文档团队优化、轻量运营直接提 PR）、文档团队优化审合并。
> **定位**：技术文档的**写法**上位规范，管中英文混排、标点、链接/代码周围间距等微观排版。与 [writing-baseline-rd.md](writing-baseline-rd.md) 的关系：本篇是完整版，研发基线是从中挑出的「研发必守子集」。
> **维护者**：文档团队。

| 修订人 | 版本 | 修订内容 | 发布时间 |
| --- | --- | --- | --- |
| 文档团队 | V1.0 | 初始定稿，同步团队 | 2026-07-27 |

## 1. 中英文之间加空格

中文与英文/数字/半角符号相邻时，中间加一个半角空格。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| RDK X3是边缘计算开发板 | RDK X3 是边缘计算开发板 |
| 使用BPU进行推理 | 使用 BPU 进行推理 |
| 支持TROS中间件 | 支持 TROS 中间件 |

> **为什么硬性？** 中英文之间没有空格时，等宽字体下挤成连续字符串，扫读要逐字定位边界；加空格后视觉上自然断词，认知负荷明显下降。这是中文技术文档的行业通行做法（参考中文文案排版指北）。

### 1.1 已知例外

| 场景 | 写法 | 说明 |
|------|------|------|
| 专有名词内部 | `RDK X3`、`RDK S100`、`TROS` | 词内部空格是产品名的一部分，不再额外加 |
| 英文缩写紧贴中文单位 | `80 TOPS`、`6 核 A78AE` | 数字与英文单位之间加空格，英文单位与中文之间也加 |
| 版本号 | `v1.0`、`5.1.0` | 不拆开加空格 |

## 2. 中文与链接/代码之间加空格

中文与行内链接、行内代码相邻时，中间加一个半角空格。

### 2.1 链接周围

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 使用[XBurn工具](./xburn.md)烧录固件 | 使用 [XBurn 工具](./xburn.md) 烧录固件 |
| 详见[准入政策](admission-policy.md)第4节 | 详见 [准入政策](admission-policy.md) 第 4 节 |
| 参考[HBM模型](...)格式 | 参考 [HBM 模型](...) 格式 |

> 链接紧贴中文时，渲染后的下划线/颜色变化会和汉字粘连，读者难以判断链接从哪到哪。加空格让链接边界清晰。

### 2.2 行内代码周围

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 执行`sudo reboot`命令 | 执行 `sudo reboot` 命令 |
| 编辑`config.yaml`文件 | 编辑 `config.yaml` 文件 |

> 反引号只包命令、文件名、代码片段等读者要按字面识别的 token；中文叙述词（「执行」「命令」「文件」）不包。行内代码用等宽字体渲染，紧贴中文时等宽字体与中文字体的基线、字宽都不一致，视觉上挤成一坨。加空格让代码片段独立成块。

### 2.3 标点紧跟不加空格

链接/代码后面如果紧跟的是标点（逗号、句号、顿号、括号等），**不加空格**——标点已经起到了视觉分隔作用：

| ❌ 多余空格 | ✅ 正确 |
|--------|-----------|
| 使用 [XBurn](./xburn.md) ，烧录固件 | 使用 [XBurn](./xburn.md)，烧录固件 |
| 执行 `sudo reboot` 。 | 执行 `sudo reboot`。 |

---

## 3. 中文与数字之间加空格

中文与阿拉伯数字相邻时，中间加一个半角空格。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 80TOPS算力 | 80 TOPS 算力 |
| 6核A78AE | 6 核 A78AE |
| 第4节 | 第 4 节 |
| 2026年7月 | 2026 年 7 月 |

> 数字与中文之间加空格的逻辑同中英文：数字是半角字符，紧贴中文时扫读断词困难。

### 3.1 已知例外

| 场景 | 写法 | 说明 |
|------|------|------|
| 百分比 | `80%` | 数字与 % 之间不加空格 |
| 范围号 | `1-8 台` / `80-120 TOPS` | 统一用半角连字符 `-`，前后不加空格。中文语境、一般数值范围（`1-8 台`）与中英混排、纯数字技术参数（`80-120 TOPS`）均用 `-`，不区分场景 |
| 序号编号 | `第 4 节` 加空格，但 `GPIO4` 这种引脚名不加 | 引脚名是固定标识符，不拆 |

> 数值范围不用波浪号 `~`、en-dash `–`、鱼尾号、一字线作分隔，统一用半角连字符 `-`。注意区分：`1-8` 表示「1 到 8」（范围），`-5` 表示「负 5」（负号），`a-b` 表示「a 减 b」（减号）——三者字符相同，语义由上下文区分，不因可能混淆而回避 `-` 作范围号。

---

## 4. 全角与半角标点

标点跟随**所在语境**，不跟随单个字符：

| 语境 | 用什么标点 | 举例 |
|------|----------|------|
| 中文段落 | 全角标点（，。、（）） | RDK X3 是开发板，算力 80 TOPS。 |
| 英文段落/代码 | 半角标点（,.()） | `Run sudo reboot.` |
| 中文段落里的英文片段 | 中文标点包外，英文内部用半角 | 支持 BPU（Brain Processing Unit）推理 |

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 支持 BPU(Brain Processing Unit)推理 | 支持 BPU（Brain Processing Unit）推理 |
| 步骤 1,安装驱动 | 步骤 1，安装驱动 |
| 详见准入政策.第 4 节 | 详见准入政策，第 4 节 |

> 判断依据：看标点**两端是不是中文**。两端中文 → 全角；两端英文/代码 → 半角；括号内容是英文但整体在中文句子里 → 用全角括号包住。

### 4.1 句末加句号

完整的句子句末加句号，不漏写。表格说明列、列表项（bullet）若为完整句子也加句号；纯标签、短语、短标题不加。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| XBurn 软件支持的同时烧录设备上限 | XBurn 软件支持的同时烧录设备上限。 |
| 多设备并发时确保每台设备供电充足 | 多设备并发时确保每台设备供电充足。 |
| 上限：8 台（标签式，非句子，不加） | 上限：8 台（保持，短语不加句号） |

> 判据：是「完整句子（有主谓或完整陈述）」还是「标签/短语」。完整句子加句号收尾，让读者明确"这句讲完了"；标签、短语、短标题不加。常见漏句号处：表格说明列、bullet 项——这两处若是完整陈述句，补句号。

---

## 5. 日期与时间写法

日期统一用 `YYYY-MM-DD` 连字符格式（ISO 8601），不用点号分隔：

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| `2026.07.14` | `2026-07-14` |
| `2026/7/14` | `2026-07-14` |

> 中文年月叙述可用 `2026 年 7 月`（中文写法）。完整日期（年月日齐全）一律用连字符 `2026-07-14`，不用点号 `2026.07.14`——点号在数字中易与小数点、句号混淆。

---

## 6. 表格写法

表格列对齐按列内容定，不整表一刀切；**表头跟该列正文同一对齐方式**（表头是对齐方式的一部分，不该单独居中或左对齐）：

| 列内容 | 对齐方式 | markdown 语法 |
| ------ | :------: | :------: |
| 文本描述、长句 | 左对齐 | `----` |
| 纯数值、需要比大小 | 右对齐 | `----:` |
| 短标记、编号、状态（✅/❌）、短代码值 | 居中 | `:----:` |

> 三种对齐的语法（`----` / `----:` / `:----:`）写在「markdown 语法」列里照抄即可——上表「对齐方式」「markdown 语法」两列居中是因它们是短标记速查类。

示例（参数表，三列都是文本，表头跟正文一起左对齐）：

| 字段 | 取值 | 说明 |
| ------ | ------ | ------ |
| 产品类型 | `RDK X5` | RDK X5 选 `RDK X5` |

### 6.1 表格宽度占满页面

表格默认占满内容区宽度（Docusaurus 默认行为），**不要**写内联样式把表格宽度收窄到固定像素。窄表视觉上留白失衡，与上下段落割裂。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 用 `<table style="width:50%">` 或 `style="width:400px"` 把表收窄 | 让表格走默认 `width:100%`，列宽由内容自适应 |

> 表格的职责是结构化对比，占满宽度让每列有舒展空间，长文本列不被挤压换行。需要控制的是列对齐和列内容，不是表格整体宽度。局部小幅截图才用 `<img>` 控宽（见图片写法），表格不控宽。

---

## 7. 操作类文档写法

烧录、刷机、工具配置等**操作类文档**是 RDK 文档的高频类型。这类文档写法不统一时，读者分不清「点什么」「填什么」「先做哪步」，照着做容易卡壳。本节给操作类文档的写法底线。

> **跨产品复制提示**：烧录类文档常从其他产品复制（如 S100 → S600）。复制残留（型号/参数/接口照搬）属**事实错误**，不是写法问题——写法规范管不了。从其他产品复制操作类文档时，研发须逐段核对，本节的写法规则在核对事实之后适用。

### 7.1 标题写法

标题风格按**读者拿这页做什么**分（Diátaxis 框架），不按页面讲什么主题分——同一主题，读者来"做/懂/修"该用不同标题风格（如"配置串口参数"是操作、"串口接口概览"是概念、"串口无输出"是排错）。分四类，按文档类型选：

| 文档类型 | 标题风格 | 举例 |
|---------|---------|------|
| 操作类（How-to / Quick start / 操作步骤） | **动词+名词**命令式短语 | 使用 XBurn 烧录系统镜像、配置串口参数、安装 USB 驱动 |
| 概念/架构类（Explanation） | **名词短语**（-ing 仅当描述过程） | TROS 中间件概览、BPU 架构、模型编译流程 |
| 故障排查类（Troubleshooting） | **问题现象**或**问句** | 烧录失败：设备未识别 / 烧录为什么失败？ |
| 参考类（Reference：API/配置/规格） | **名词短语**，函数/选项名原样 | BPU API、配置选项、`sp_init_bpu_module` |

**操作类标题用动词+名词**（最常见的类型，展开讲）：

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 烧录步骤 | 使用 XBurn 烧录系统镜像 |
| 串口配置 | 配置串口参数 |
| 驱动安装 | 安装 USB 驱动 |
| 系统启动 | 启动系统 |
| 镜像下载 | 下载系统镜像 |

**判据**：问「这个标题是读者要*做*的事，还是*关于*某事」——操作类标题该是前者，用动词开头。

> **行业依据**：参考 Diátaxis 框架与 Google / Microsoft 开发者文档风格指南——how-to 标题用祈使语气（imperative mood），以动词开头，描述读者要达成的动作。中文无词形上的祈使句，等价结构即「动词+宾语」命令式短语（如「烧录系统镜像」「配置串口参数」）。
>
> 名词短语标题（如「烧录步骤」「串口配置」）读起来像目录分类，不表达动作，读者扫侧边栏时看不出「这页教我做什么」。动词+名词把目标动作前置，扫读即知任务。与英文规范 [writing-style-guide-en.md](writing-style-guide-en.md) §1.1 对应（英文用 bare imperative，中文用动词+名词）。
>
> **侧边栏分组名（`_category_.json` 的 label）例外**：侧边栏分组是导航分类，可用名词短语（如「烧录步骤」）；但页面 H1 仍用动词+名词。两者职能不同，不必一致。

**标题不手写编号**：只要是标题 都不写 `1.2.1.1`、`1.7` 这类点号编号前缀——Docusaurus 侧边栏自动编号，手写会撞号、页面移动时漂移。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| `# 1.2.1.1 全镜像烧录` | `# 烧录全镜像` |
| `## 1.7 下载资源汇总` | `## 下载资源` |

**标题不用 emoji**：标题和页面标题不放 emoji（🖥️/⚙️ 等）——破坏屏幕阅读器、干扰 CAT 翻译、侧边栏渲染不一致。需要视觉提示用 admonition（`:::info`）或图标组件，不在标题文字里塞 emoji。

> 标题写法的另两条——单 H1（MD025，每页一个 `#`）、标题层级递进（MD001，不跳级）——属格式类，CI 用 markdownlint 拦（见 §8.4）。本条只讲标题文字怎么写，不讲层级结构。

### 7.2 控件加粗，选项值用行内代码

UI 控件名（菜单 / 下拉框 / 按钮 / 勾选项 / 标签 / 页面名）**加粗**；用户在该控件里选的具体值或填的内容用 `行内代码`。

**判据**：问「这是界面上的可点控件，还是控件里选的值」——控件加粗，值加代码。

| 元素 | 写法 | 举例 |
|------|------|------|
| UI 控件名 | **加粗** | **产品型号**、**下载模式**、**开始升级**、**高级配置** |
| 控件里选/填的值 | `行内代码` | `RDK S100`、`DFU+Fastboot`、`USB`、`eMMC`、`secure` |

操作动词用标准写法，避免口语：

| 操作动词 | 标准写法 | 说明 |
|---------|---------|------|
| 鼠标左键单击 | 单击 | 默认动作，不写"左键单击"或"点击" |
| 鼠标右键单击 | 右键单击 | 触发右键菜单 |
| 鼠标左键连按两下 | 双击 | 不写"连点两下" |
| 鼠标指针停在元素上 | 悬停 | 触发 tooltip 或下拉 |
| 选项/值的选取 | 选择 | 下拉框选值、单选取值 |
| 复选框打勾 | 勾选 | 不写"打勾""勾上" |
| 输入框填字 | 输入 | 不写"键入""填写" |
| 折叠项展开 | 展开 | |

**操作句式**：动词（不加粗）+ 控件（加粗）+ 值（代码）。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 单击`开始升级`，设备上电 | 单击 **开始升级**，设备上电 |
| `产品型号`选择`RDKS100` | **产品型号** 选择 `RDK S100` |
| 单击**开始升级**，选择 **usb** 连接 | 单击 **开始升级**，**连接模式** 选择 `USB` |
| 展开`高级配置`，勾选`烧录指定区域` | 展开 **高级配置**，勾选 **烧录指定区域** |

> 加粗标记「界面上一个可识别的可点控件」，行内代码标记「要选/填的具体值」。两者视觉区分让读者一眼分清「点什么」和「选什么」。操作动词不强调，避免整句满屏加粗。

**行内中文加粗两侧加空格**：加粗是半角字符，紧贴中文时扫读断词困难（同中英文混排加空格逻辑）。正文中行内中文加粗的界面词，两侧加单个半角空格；若加粗紧邻标点（逗号、句号、顿号等），则省略空格（标点已起分隔作用）。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 单击**产品类型**选`RDK X5` | 单击 **产品类型** 选 `RDK X5` |
| 展开**高级配置**、勾选**烧录指定区域** | 展开 **高级配置**、勾选 **烧录指定区域**（加粗紧邻顿号，省略空格） |

### 7.3 步骤带操作顺序

操作步骤用**有序列表**（1. 2. 3.），同一任务内的子步骤连续编号，体现先做哪步、再做哪步。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 用无序列表堆「设置项」，再另起一段写「点击开始」 | 步骤连续编号：1. 选型号 → 2. 选模式 → 3. 点击开始升级 |

> 操作类文档的核心是「照着做能跑通」。顺序错一步可能卡住，所以必须用有序列表明示先后，不用无序列表模糊顺序。

**子标题编号**：步骤多到要分阶段、用子标题分节时，子标题带不带"步骤一/二"看子标题是**分阶段步骤**还是**并列 section**。

| 子标题类型 | 编号写法 | 举例 |
|---------|---------|------|
| **分阶段步骤**（每个标题是一阶段动作，合起来完成一个任务） | **写顺序词**"步骤一/二/三"显先后 | `## 步骤一：进入 DFU 模式`（底下可接 1.2.3. 子步骤）→ `## 步骤二：配置烧录参数` → `## 步骤三：执行烧录` |
| **并列 section**（每个标题是文档的一个组成块，非动作阶段） | **不编号**——写概念名即可 | `## 前提条件` → `## 操作步骤` → `## 常见问题` |

> **编号约束**：`步骤一`、`步骤二`作为标题须在**一页内全局唯一**（不重复出现同一个"步骤一"）。重复会让 github-slugger 生成撞名锚点、页内跳转失准。MD024 不在 CI 开启范围（人工约束），靠写时自查。

### 7.4 删冗余引导语、修不当并列

**删冗余引导语**：「设置方法如下：」「打开 XBurn，设置方法如下：」这类套话直接删，接步骤即可——步骤本身已说明「怎么设置」，引导语只会增加噪声。

**修不当并列**：物理操作（拨码进 DFU/Fastboot）和工具操作（在 XBurn 配置烧录）不是同级并列，是**先后顺序**——用编号子标题体现先后，不要平铺成两个加粗伪标题让读者以为二选一。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 「设置方法如下：」后接步骤 | 直接接步骤，删引导语 |
| 「**进入 DFU 模式**」「**使用 XBurn 烧录**」两个加粗伪标题并列 | 用子标题分节、按先后从上到下排列 |

> 并列伪标题会让读者以为「二选一」，实际是「先做 A 再做 B」。改成真实子标题按先后排列即可——子标题带不带「步骤一/二」按它是分阶段步骤还是并列 section 定（见 §7.3）。
>
> **所有标题层级（H1/H2/H3/H4）都不手写点号编号**（`1.2.1.1`、`1.7`、`## 3.`、`### 3.1` 这类）——点号会和 Docusaurus 侧边栏自动编号撞、页面移动时漂移（见 [writing-style-guide-en.md](writing-style-guide-en.md) §2.1）。有顺序的步骤标题用"步骤一/二"这种顺序词，不用数字点号。

### 7.5 模糊表述要写实

「数据传输质量高」「长度越短越好」这类模糊表述，读者无法判断该怎么做。要写成**可判定的属性**。

| ❌ 模糊 | ✅ 写实 |
|--------|--------|
| 数据传输质量高的线材 | 支持数据传输（非纯充电线） |
| 长度越短越好 | 建议 1 米以内 |
| 稳定的电源 | 5V/2A 以上电源适配器 |

> 写实的标准是「读者照着这个描述能选出唯一正确的物件/做出唯一正确的动作」。模糊表述把判断成本转嫁给读者。

### 7.6 硬件标识符

拨码开关（SW1/SW3）、按键（K1）、接口（Type-C/J16）、指示灯（DOWNLOAD）等硬件标识符，与软件 UI 控件性质不同——是物理实体或板载丝印，不是 GUI 里能点的对象。

**判据**：硬件标识符用**普通文本**，不加粗、不包行内代码——**加粗留给屏幕上可点的 GUI 控件（**开始升级**），板子上要找的物理标识（SW1）用普通文本**。读者扫读时凭"编号 vs 按钮词"区分"去屏幕找"还是"去板子找"，不靠字体。

| 元素 | 标记 | 正例 | 反例 |
|------|------|------|------|
| 板载元件编号 | 普通文本 | 将 SW1 拨码至 ↑ | 将 **SW1** 拨码（误用加粗，与 UI 控件混淆）/ 将 `SW1` 拨码（编号不必包代码） |
| 丝印功能词 | 普通文本 | 检查 DOWNLOAD 指示灯 | 检查 **DOWNLOAD** 指示灯 / 检查 `DOWNLOAD` 指示灯 |
| 编号 + 描述后缀 | 普通文本 | 按下 K1 复位按键 | 按下 **K1** 复位按键 / 按下 `K1` 复位按键 |
| 拨码位状态值 / 寄存器值 | `行内代码` | SW8 拨至 NVMe 启动位（`D12=OFF, D13=ON`） | SW8 拨至 NVMe 启动位（D12=OFF, D13=ON） |

> **为什么编号用普通文本而非行内代码**：硬件编号（SW1/K1）是便于指代的代号，不是要用户"照字面敲入"的 token（区别于命令 `sudo reboot`、文件名 `config.yaml`——那些要逐字输入，用代码字体标示"按字面识别"）。编号只是板子上的标识，裸文本读着通顺，加代码反而让全文满屏反引号。

> **链接到硬件页**：硬件标识符首次出现时，链接到产品硬件介绍页（如 `[SW1](../hardware-intro#开关-sw1sw2)`），锚文字保持普通文本。

> 改一份操作类文档时，同模式产品（如 RDK S100/S600 burn.md）要同步查有无分叉。

### 7.7 图片写法：HTML `<img>` 为主

图片统一用 HTML `<img>` 标签 + JSX style 控制宽度，**不用 Markdown `![]()`**。理由：Docusaurus 用 React 渲染，OSS 原图普遍偏大，Markdown 语法无法控制显示尺寸会撑爆页面；HTML `<img>` 能控宽度。仓库现有 500+ 处图片已是此写法，本规范追认现实。

**标准写法**：

```jsx
<img src="https://rdk-doc.oss-cn-beijing.aliyuncs.com/doc/img/{product}/{大类}/xxx.png" alt="XBurn 主界面配置参数" style={{ width: '100%' }} />
```

| 属性 | 要求 |
| ------ | ------ |
| `src` | 自有 OSS URL（`rdk-doc.oss-cn-beijing.aliyuncs.com`），禁用外部图床/CDN。OSS 路径镜像到大类 `/doc/img/{product}/{大类}/xxx.png`，图片不入仓 |
| `alt` | **非空**，写图片表达的内容（如 `alt="XBurn 主界面配置参数"`），满足 MD045 无障碍要求。不写 `alt=""` |
| `style={{ width: '...' }}` | 控制显示宽度。**默认 `100%`**；小图标/局部截图用 `60%`；按钮/指示灯等小幅元素用 `≤20%` |
| 自闭合 | JSX 必须自闭合 `/>`，不能省略斜杠 |

**图片文件命名**：lower-kebab-case，功能名为主（`xburn-main.png`、`board-dfu-mode.png`），不用 `步骤1.png`/纯日期戳。同一界面更新覆盖同名，OSS 版本管理兜底历史。

**关键步骤配截图，非必要不配**：操作类文档按**能否照做**决定配不配截图——没截图用户找不到下一步点哪、填什么的步骤必须配；文字 + 代码能讲清的不配，避免迭代增多时截图维护工作量爆炸。不留 `📷`/`SCREENSHOT`/`IMAGE PENDING` 文本占位符，不留空图片。

**判据**：问「去掉这张截图，用户照着文字和代码，还能不能找到下一步该点哪、填什么」——能找到 → 不配；找不到、或要靠截图才能定位界面控件 → 必须配。

> **截图两层作用分开看**：**导航作用**（指路）决定"配不配"——没截图照做不了才配，这是上面判据看的层；**证据作用**（证明跑通）决定"配了之后的质量"——一旦判定要配，就必须是真跑通的截图，不许旧图/网图/占位符。"关键步骤必须配"管证据层、"非必要不配"管导航层，各管各的，让截图集中在少数真正需要的步骤上。

### 7.8 句式：直接描述，避免把字句/将字句

步骤与动作描述用**直接陈述句**，避免"把""将"字句。把字句/将字句把对象前置、动词后置，读着绕、且像操作手册的套话；直接描述主语+动作更利落。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 将 PC 的 USB 接口与设备的 Type-C 接口相连接 | 通过 Type-C 数据线连接 PC 的 USB 口与设备的 Type-C 口 |
| 把镜像写入 SD 卡 | 镜像写入 SD 卡 或 将镜像烧录到 SD 卡（动词直接带对象） |
| 把开发板连接到 PC | 设备连接到 PC |

**判据**：能不能去掉"把/将"、让主语直接做动作。能去掉就去掉，句子更直接。仅当"把/将"后接长定语、去掉会歧义时保留（如"将已解压的 product 文件夹选为镜像目录"）。

### 7.9 提示框 / Admonition 用法

Docusaurus 的 `:::note/info/tip/warning/danger` 沿袭 GitHub/Diátaxis 约定，按**后果严重程度递增**：note < info < tip < warning < danger。

| 类型 | 何时用 | 词性 | 一句话判据 |
|------|--------|------|-----------|
| `:::note` | 背景知识、可选信息，删掉不影响完成任务 | 中性 | "了解一下" |
| `:::info` | 补充的事实信息（配置值、链接、参数说明） | 中性 | "补充事实" |
| `:::tip` | 推荐做法、省事技巧、最佳实践 | 推荐 | "建议你这么做" |
| `:::warning` | 易错点、可能出问题（通常可恢复） | 警示 | "做错有后果" |
| `:::danger` | 严重不可逆后果（烧坏设备、数据永久丢失） | 严重警示 | "严重不可逆" |

**用法约束**：
- **不滥用**：满屏框会让读者视觉疲劳、全忽略。正文能讲清的别用框，框留给"真正需要突出"的内容。
- **标题可选但建议写**：`:::warning 标题`，标题简短点题；不写时 Docusaurus 用默认（"注意/警告"等）。
- **内容自包含**：框内信息要完整，别让读者跳出去才能懂。
- **FAQ/排错不塞框**：有专门页就指向专门页（如"见 [串口与驱动问题](../troubleshooting/serial-driver)"），不在 tip 里复述。
- **内部口径不进用户手册**：`:::info 待确认` 这类"待研发补充"的内部标记不写进线上手册（要么补全内容、要么删掉该框）。

### 7.10 路径与导航用 `>` 分隔

表示"从一处到另一处"的路径导航（文件路径、目录层级、UI 菜单点击路径、网页内逐级查找）用 `>` 分隔，**不用短横线 `-`**——`-` 与产品名/版本号/文件名里的连字符撞，读者分不清哪段是层级、哪段是名字本身。

**写法**：每段按类型分——菜单名加粗（GUI 控件，按 §7.2）、文件名/目录名用行内代码（按字面识别的 token），`>` 两侧加半角空格。

```markdown
依次进入 `RDK LNX SDK` > `firmware` > `product.zip`
按 **设置** > **高级配置** > **烧录指定区域** 路径操作
```

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 选择最新版本的镜像-`RDK LNX SDK`-`firmware`-`product.zip` | 选择最新版本，依次进入 `RDK LNX SDK` > `firmware` > `product.zip` |
| 单击 设置-高级配置-烧录指定区域 | 单击 **设置** > **高级配置** > **烧录指定区域** |

**判据**：是不是"层级递进 / 逐级查找"。是：用 `>`；不是（同一层级的并列项）：用顿号或 `、`。

### 7.11 步骤中的异常分支：用 admonition 包，不与步骤平级

操作步骤默认讲**成功/主流路径**。若某步可能出现"不成功"的情况（如识别不到设备、报错、未达预期结果），异常分支用 `:::note`/`:::warning` 包住，挂在对应步骤后，**不与步骤平级平铺**。

**判据**：问「这是主流步骤，还是"如果不成功怎么办"」——主流进步骤编号、异常进 admonition 挂该步骤后。

| ❌ 不允许 | ✅ 应该 |
|---------|--------|
| 步骤 6 成功 + 图，然后平铺一段"若提示 USB download gadget 未识别，回安装驱动重装" + 失败图（和步骤同级，打断主线） | 步骤 6 成功 + 图，其后用 `:::note 驱动未识别` 包住异常文字 + 失败图（缩进属该步骤、视觉隔离） |

**写法**：

- 主流步骤只讲成功路径，干净顺畅——顺利的用户照做即通，不被"如果不成功"噪声打断。
- 异常分支挂在**对应步骤后**、缩进 3 空格，让 admonition 视觉上"属于该步骤的分支"，而非独立段落。
- admonition 类型按需选（note 补充/可选、warning 易错有后果）。
- 异常内容两种：**就地讲**（高频、值得这页讲清）或**指向排错页**（低频、复述）——别既就地讲又整段复述排错页内容。

> 行业依据：操作类文档的主流是"照着做能成"，异常是少数分支。把异常和步骤平级，顺利的读者也被迫读一堆"如果不成功"，噪声大、扫读断。admonition 隔离异常、挂在对应步骤后，顺利者不展开、遇到才看——主线干净、异常可及。
>
> 这条与步骤顺序、admonition 用法配套：那两节讲步骤怎么排、框选哪种，本条讲"步骤里的异常怎么放"。

---

## 8. 执行与落地

### 8.1 现阶段：报告级，人工巡检

本规范的所有条款**现阶段不进 CI**——markdownlint 与 custom-checks 不检查中英文空格、标点这类微观排版（它们只拦格式可用性，见 [ci-pipeline.md](ci-pipeline.md) §2.2）。

| 谁执行 | 怎么执行 |
|--------|---------|
| 文档团队（深度运营优化时） | 优化成品时按本规范统一，作为质量兜底的一部分 |
| 研发（轻量运营提 PR 时） | **不强制**——轻量运营文档团队不深度优化（技术准确性研发兜底，见 [writing-baseline-rd.md](writing-baseline-rd.md)），研发基线里也未列入必守项 |
| 文档团队巡检 | 定期巡检存量文档，按本规范纠偏 |

> **为什么研发不强制？** 中英文空格这类规则研发记不住、也无人拦（CI 不查），写进研发基线会变成「必守但无人执行」的死规则，违反 [writing-baseline-rd.md](writing-baseline-rd.md) 「研发照模板填基本就过、不在格式上耗精力」的初衷。这类规则的统一由文档团队在优化/巡检时兜底。

> **为什么不一次写全**：现阶段报告级人工巡检，规则越多巡检成本越高、收益边际递减。本规范先覆盖扫读影响最大的 7 类（中英文空格、标点、日期、表格、操作类写法），强调用法/列表写法/代码块空行/引用块/表格内标点等待 Vale 第三阶段落地时按需扩充——规则与执行机制同步成熟，避免「写了规则没人执行」。

### 8.2 未来：Vale 第三阶段落地后转 CI 报告

按 [ci-pipeline.md](ci-pipeline.md) §2.2，Vale 写作规范层是**第三阶段**（优先级最低，需先沉淀术语表与风格规则）。Vale 上线后，本规范条款（中英文空格、链接间距等）转为 CI **报告**（不阻断合并），规则成熟后再从报告转阻断。在 Vale 落地前，本规范是人工巡检依据，不是 CI 门控——条款写在这里是为 Vale 提供规则源。

### 8.3 与其他规范的关系

| 规范 | 管什么 | 与本篇关系 |
|------|--------|----------|
| [writing-baseline-rd.md](writing-baseline-rd.md) | 研发必守的**事实准确性 + 信息完整度**子集 | 本篇是上位完整版，研发基线不重复本篇条款 |
| [glossary.json](glossary.json) | 术语**叫什么**（统一用词、大小写） | 本篇管术语**怎么排**（周围空格、标点），不管术语本身 |
| [ci-pipeline.md](ci-pipeline.md) | CI 检查哪些、怎么拦 | 本篇说明哪些条款进 CI、哪个阶段进 |
| [url-governance.md](url-governance.md) | 链接**指向哪里**（URL 策略） | 本篇管链接**周围怎么排**（空格），不管 URL 本身 |

### 8.4 CI 规则 ↔ 规范条款对照

CI 不是凭空抽规矩——它查的每一条，规范里都立了对应的写法条款。下表把「CI 查什么」对齐到「规范哪节讲写法」「CI 哪节讲执行」，避免「CI 查的没在规范写明」的脱节。规范立规矩，CI 抽可机器判定且值得拦合并的子集去执行。

| CI 查的 | 规范写法条款 | CI 执行 | 现在进 CI 没 |
|---------|------------|--------|-----------|
| 标题层级递进（MD001） | §7.1（标题不手写编号） | [ci-pipeline §2.2](ci-pipeline.md) | ✅ markdownlint |
| 单 H1（MD025） | §7.1 | [ci-pipeline §2.2](ci-pipeline.md) | ✅ markdownlint |
| 代码块标语言（MD040） | §2.3 | [ci-pipeline §2.2](ci-pipeline.md) | ✅ markdownlint |
| 空链接（MD042） | §2 | [ci-pipeline §2.2](ci-pipeline.md) | ✅ markdownlint |
| 图片 alt 非空（MD045） | §7.7 | [ci-pipeline §2.2](ci-pipeline.md) | ✅ markdownlint |
| 文件名/目录名 lower-kebab-case | [url-governance §2](url-governance.md) | [ci-pipeline §2.2](ci-pipeline.md) | ✅ custom-checks |
| frontmatter 覆盖率（title/description） | 模板 frontmatter 占位字段 | [ci-pipeline §2.2](ci-pipeline.md) | ✅ custom-checks |
| 占位符/遗留标记 | §2.2、[baseline-rd §2.2](writing-baseline-rd.md) | [ci-pipeline §2.2](ci-pipeline.md) | ✅ custom-checks |
| 链接不带 `.md` 后缀 | §2、[url-governance §8](url-governance.md) | docusaurus build 解析 | ✅ build（约定兜底） |
| 中英文空格、标点、术语大小写 | §1-§7、[glossary.json](glossary.json) | Vale（待第三阶段） | ❌ 待 Vale |

> **三档，不是两档**：① markdownlint 5 条 + custom-checks（上表前 8 行）现已在 CI 拦合并；② 中英文空格/标点/术语大小写等写作风格条款（末行）待 Vale 第三阶段转报告、成熟后转阻断；③ 链接后缀靠约定 + build 解析兜底（markdownlint 不查）。规范立规矩在前，CI 按可机器判定性分档跟进——不是所有规矩都进 CI，进 CI 的都有规范对应条款。
>
> markdownlint 当前只开 5 条，规范里的格式规矩远不止（中英文空格、标点、表格对齐、链接间距等都不在 5 条内）——这些没进 CI 的规矩仍由文档团队巡检兜底（见 §8.1），并非"CI 查了但规范没写"。

---

## 9. 起步范围说明

本规范当前的覆盖范围：

1. 中英文之间加空格（§1）
2. 中文与链接/代码之间加空格（§2）
3. 中文与数字之间加空格（§3）
4. 全角与半角标点（§4）
5. 日期与时间写法（§5）
6. 表格写法（§6）
7. 操作类文档写法（§7，**试行**）——标题写法（§7.1，含四类标题/不手写编号/不用 emoji）、控件加粗/选项值代码、步骤带顺序、删冗余引导语、修不当并列、模糊表述写实、硬件标识符、图片 HTML `<img>`、句式避把/将字句、Admonition 用法、路径用 `>` 分隔、步骤异常分支用 admonition 包，共 §7.1-§7.11
8. 执行与落地（§8）——含 §8.4 CI 规则 ↔ 规范条款对照

以下内容**暂未纳入**，待 Vale 第三阶段落地时按需补充：

- 强调用法（`**加粗**` vs `*斜体*` 的使用边界）
- 列表写法（有序/无序列表、嵌套层级的统一）
- 代码块前后空行
- 引用块（blockquote `>`）的写法约定
- 表格内标点（对齐已见 §6）

> 不一次写全的原因：现阶段是报告级人工巡检，规则越多、巡检成本越高，而收益边际递减。先把扫读影响最大的几类定下来，Vale 落地时再扩充——规则与执行机制同步成熟，避免「写了规则没人执行」。
